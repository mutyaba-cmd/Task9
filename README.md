# GeneralObjectsProject

Demo Java project showing:
- an abstract base class `GeneralObject`
- three concrete subclasses: `Book`, `Electronic`, `Furniture`
- `MainApp` to create instances, display details, and compute values.

To compile:
javac *.java

To run:
java MainApp
